#include <fstream>
#include <mutex>

static std::mutex g_depthDbgMutex;

static void depthDbgLog(const std::string& msg)
{
  std::lock_guard<std::mutex> lock(g_depthDbgMutex);
  std::ofstream ofs("depth_mismatch_debug.log", std::ios::app);
  ofs << msg << '\n';
}






std::ostringstream oss;
oss << "[ENC_CU] POC=" << cs.slice->getPOC()
    << " x=" << cu.Y().x
    << " y=" << cu.Y().y
    << " w=" << cu.Y().width
    << " h=" << cu.Y().height
    << " skip=" << cu.skip
    << " merge=" << cu.mergeFlag
    << " affine=" << cu.affine
    << " geo=" << cu.geoFlag
    << " interDir=" << (int)cu.interDir
    << " refL0=" << cu.refIdx[REF_PIC_LIST_0]
    << " mvL0=(" << cu.mv[REF_PIC_LIST_0].hor << "," << cu.mv[REF_PIC_LIST_0].ver << ")";

depthDbgLog(oss.str());











std::ofstream ofs("enc_depth_debug.log", std::ios::app);












Please make a plan first before editing code.

Goal:
Currently, when building merge candidate lists, a neighboring block coded with the camera projection tool is treated as unavailable. We want to change this. A camera projection block already has per-pixel projected motion, so it should export usable motion information for both regular merge and affine merge candidates.

High-level requirement:
Do not treat camera projection blocks as unavailable in merge candidate derivation. Instead, derive and store motion information from the camera projection result.

Reference code locations to inspect:

1. Regular merge candidate derivation

* CommonLib/UnitTools.cpp
* PU::getInterMergeCandidates()
* Around lines 2252–2485
* This function checks neighboring CUs, reads getMotionInfo(pos), and fills:
    * mrgCtx.interDirNeighbours[cnt]
    * mrgCtx.mvFieldNeighbours[cnt][RPL0/RPL1]
    * mrgCtx.bcwIdx[cnt]
    * mrgCtx.LICFlags[cnt]

2. MotionInfo structure

* CommonLib/MotionInfo.h
* MotionInfo
* Around lines 110–121
* Important fields:
    * mv[NUM_RPL01]
    * refIdx[NUM_RPL01]
    * isInter
    * interDir
    * sliceIdx
    * usesLIC

3. Motion buffer population

* CommonLib/UnitTools.cpp
* PU::spanMotionInfo()
* Around lines 7240–7325
* Current behavior:
    * non-affine CU: fills the whole CU with one MotionInfo
    * affine CU: writes per-4x4/subblock motion into cu.getMotionBuf()
* For camera projection blocks, add a custom path that writes precomputed 4x4 camera-projection MotionInfo directly into the motion buffer.

4. MergeCtx application

* CommonLib/InterPrediction.cpp
* MergeCtx::setMergeInfo()
* Around lines 5903–5943
* This shows how a selected regular merge candidate is applied to CU fields:
    * cu.interDir
    * cu.mv[]
    * cu.refIdx[]
    * cu.mergeType
    * cu.bcwIdx

5. Affine merge candidate derivation

* CommonLib/UnitTools.cpp
* PU::getAffineMergeCand()
* Around lines 6167 onward
* Important nearby logic:
    * candidate initialization: around lines 6171–6185
    * affine control point collection from neighbors: around lines 6429–6579
    * inherited affine candidate filling: around lines 6328–6354

6. AffineMergeCtx structure

* CommonLib/InterPrediction.h
* AffineMergeCtx
* Around lines 91–117
* Important fields:
    * mvFieldNeighbours[cand][cp][RPL0/RPL1]
    * interDirNeighbours[cand]
    * affineType[cand]
    * bcwIdx[cand]
    * LICFlags[cand]
    * numValidMergeCand

7. Applying affine MV fields

* CommonLib/UnitTools.cpp
* PU::setAllAffineMvField()
* Around lines 6909–6925
* PU::setAllAffineMv()
* Around lines 6927–6990
* This shows how LT/RT/LB affine control-point MVs become per-4x4 motion.

Proposed implementation plan:

A. Add a camera-projection motion cache
For each camera projection coded block, compute and store motion information at 4x4 granularity.

For each 4x4 subblock:

* sample projected displacement at the four luma corner pixels
* average the four displacement vectors
* convert the averaged displacement into VTM Mv internal precision
* store:
    * MotionInfo::isInter = true
    * MotionInfo::interDir
    * MotionInfo::mv[RPL0/RPL1]
    * MotionInfo::refIdx[RPL0/RPL1]
    * MotionInfo::sliceIdx
    * MotionInfo::usesLIC = false unless the camera projection mode explicitly supports LIC

B. Modify motion buffer spanning
In PU::spanMotionInfo() or a helper called from it, add a camera-projection branch:

* if the CU uses camera projection mode:
    * do not call mb.fill(mi)
    * instead copy the precomputed 4x4 camera-projection MotionInfo into cu.getMotionBuf()
* this allows neighboring blocks to use getMotionInfo(pos) normally.

C. Regular merge support
Modify the existing logic that currently marks camera projection neighbors unavailable.
Instead:

* allow camera projection CUs as inter neighbors
* rely on puNeigh->getMotionInfo(pos) to return the 4x4 camera-projection MV at the queried position
* keep the existing MergeCtx filling logic unchanged as much as possible.

D. Affine merge support
Add a camera-projection affine candidate path.

For a camera projection block, derive 3 control-point MVs:

* LT: projected motion near the top-left corner
* RT: projected motion near the top-right corner
* LB: projected motion near the bottom-left corner

Prefer using averaged local corner samples or nearby 4x4 cache entries, not a single noisy pixel.

Then add a candidate into AffineMergeCtx:

* affMrgCtx.mvFieldNeighbours[cand][0][list] = LT
* affMrgCtx.mvFieldNeighbours[cand][1][list] = RT
* affMrgCtx.mvFieldNeighbours[cand][2][list] = LB
* affMrgCtx.interDirNeighbours[cand] = cameraProjInterDir
* affMrgCtx.affineType[cand] = AffineModel::_6_PARAMS if LT/RT/LB are all valid
* set refIdx for each list consistently
* set bcwIdx = BCW_DEFAULT
* set LICFlags = false
* run the same redundancy/similarity checks used by existing affine merge candidates where possible.

Recommended design:

* Add helper functions instead of scattering camera-projection logic:
    * bool CU::isCameraProj(const CodingUnit&) or equivalent existing flag check
    * bool PU::getCameraProjMotionInfo(const CodingUnit&, const Position&, MotionInfo&)
    * void PU::spanCameraProjMotionInfo(CodingUnit&)
    * bool PU::addCameraProjAffineMergeCand(const CodingUnit&, AffineMergeCtx&, const CodingUnit* cameraProjNeigh, Position queryPos)

Important constraints:

* Keep encoder and decoder derivation deterministic.
* Do not introduce encoder-only motion data into decoder-side merge derivation unless it is fully derivable from decoded camera parameters/depth/reference information.
* Make sure the refIdx and interDir are valid for the current slice.
* Respect slice.isInterB() when filling RPL1.
* Use NOT_VALID and zero MV for unavailable lists.
* Keep regular merge and affine merge changes localized.
* Add CHECKs for invalid refIdx/interDir and mismatched affine CP refIdx.
* Build first, then test with camera projection mode enabled and verify that neighboring regular merge and affine merge candidates can now see the camera-projection block.

Please output:

1. A short implementation plan.
2. The exact files/functions to modify.
3. Any risks or assumptions before making code changes.





